node_modules
.next
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

*.tsbuildinfo

# Talos runtime state — regenerated on each box; not source.
.talos-swarm.json
.talos-decisions.json

# private keys / keystores — never commit
*.key
*.keystore
